import { reusableImageFields } from "../fields/reusable-image"

export const reusableImage = {
  name: "reusableImage",
  type: "document",
  title: "Reusable Image",
  fields: reusableImageFields,
}
