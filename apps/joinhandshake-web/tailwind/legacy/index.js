const { legacyColors } = require("./colors")
const { legacyScreens } = require("./screens")
const { legacySpacing } = require("./spacing")
const { legacyFontSize } = require("./font-size")
const { legacyBorderRadius } = require("./border-radius")
const { legacyLineHeight } = require("./line-height")
const { legacyLetterSpacing } = require("./letter-spacing")

module.exports = {
  colors: legacyColors,
  screens: legacyScreens,
  spacing: legacySpacing,
  fontSize: legacyFontSize,
  borderRadius: legacyBorderRadius,
  lineHeight: legacyLineHeight,
  letterSpacing: legacyLetterSpacing,
}
